#+title: Readme

Chugo is a [[https://github.com/gohugoio/hugo][Hugo]] theme I'm working on for [[https://github.com/chumutt/dogboner.xyz][my website]], which is also a work in progress.

* TODO Features

* TODO Installation

* Scripts

** Test with draft posts ON, navigating to new changes

#+begin_src sh :shebang "#!/usr/bin/env sh" :tangle ./test.sh

# Run preview server, building and previewing draft posts
# alongside published ("done") posts, rebuilding on every
# change that is made to source files, and then navigates
# to the page on the locally hosted test site that you
# changed following said change.

hugo server -D --disableFastRender --navigateToChanged
#+end_src

** Test with draft posts OFF, navigating to any new change

#+begin_src sh :shebang "#!/usr/bin/env sh" :tangle ./test-nodrafts.sh

# Run preview server, building and previewing the site with
# published ("done") posts, rebuilding on every change
# that is made to source files, and then navigates
# to the page on the locally hosted test site that you
# changed following said change.

hugo server --disableFastRender --navigateToChanged
#+end_src

** Test and clean up

#+begin_src sh :shebang "#!/usr/bin/env sh" :tangle ./test-cleanup.sh

# Run preview server, building and previewing the site with
# published ("done") posts, rebuilding on every change
# that is made to source files, and then navigates
# to the page on the locally hosted test site that you
# changed following said change. Cleans up.

hugo server --disableFastRender --navigateToChanged --cleanDestinationDir
#+end_src

** Test .css file changes

Changes to .css files won't update in previews unless you run ~hugo serve~:

#+begin_src sh :shebang "#!/usr/bin/env sh" :tangle test-serve.sh
hugo serve
#+end_src

* Configuration

** Site Configuration

#+begin_src toml :tangle ./hugo.toml
baseURL = 'https://example.org/'
languageCode = 'en-us'
title = 'My New Hugo Site'

[[menus.main]]
name = 'Home'
pageRef = '/'
weight = 10

[[menus.main]]
name = 'Posts'
pageRef = '/posts'
weight = 20

[[menus.main]]
name = 'Categories'
pageRef = '/categories'
weight = 30

[[menus.main]]
name = 'Tags'
pageRef = '/tags'
weight = 40

[module]
  [module.hugoVersion]
    extended = false
    min = "0.116.0"

[params]
  [params.author]
    name = 'Ben Bitdiddle'
    email = 'benbitdiddle@faketestemail.arpa'
  [params.copyright]
    type = 'Copyright'
    # type = '(no)Copyright'
    # type = 'CC0 1.0 Public Domain: This work has been dedicated to the public domain under the CC0 1.0 Universal Public Domain Dedication.'
    # type = 'CC BY-SA (Attribution-ShareAlike): Requires credit and sharing under the same license.'
    # type = 'CC BY-NC (Attribution-NonCommercial): Allows use but not for commercial purposes.'
    # type = 'CC BY-ND (Attribution-NoDerivatives): Allows sharing but not modifications'
    # type = 'or whatever you like'
    symbol = '©'
    # symbol = '🄯'
    preposition = 'by'
    name = 'John Doe.' # Your name followed by a period.
    # statement = 'All rights reserved.'
    statement = ''
#+end_src

** Theme Configuration

#+begin_src toml :tangle ./theme.toml
name = 'chugo'
license = 'MIT'
licenselink = 'https://github.com/chumutt/chugo/LICENSE'
description = 'a hugo theme by chu the pup'
homepage = 'https://github.com/chumutt/chugo'

[author]
  name = 'chu the pup'

[original]
  author = 'vinliao'
  repo = 'https://github.com/vinliao/hugo-starter.git'
#+end_src
** main.css
#+begin_src css :tangle ./assets/css/main.css
/* ghost.css */
body {margin:auto;
      width:80%;
      padding-top:25px;
      padding-bottom:8px;
      background-color: #18191C;
      color: #FFFFFF;
      font-family: sans-serif}

#center {text-align:center;}

a {color: #ffe620;}

a:hover {color: #c5d5d5;}

a.articlelist {color: #485978;
               font-size: 1.25em;
               font-family: monospace;
               text-align:center;}

a.articlelist:hover {color: #c5d5d5;}

a.sectionlink {color: #485978;
               font-size: 1.15em;
               font-family: monospace;
               font-weight: 700;}

a.sectionlink:hover {color: #c5d5d5;}

a.intable {color: #aca4f6;}

a.intable:hover {color: #c5d5d5;}

.spoiler {color:#18191C;}

.spoiler:hover {color: #FFFFFF;}

ul {margin-left: 10px;
    margin-right: 10px;
    padding-right: 40px;}

li {margin-bottom: 3px;
    list-style-position: inside;}

img {max-width:100%}

img.center {display:block;
            margin:auto;
            min-width:400px;
            min-height:400px;}

img.center_noresize {display:block;
                     margin:auto;}

img.vidya {max-width:400px;
           max-height:400px;
           padding:10px;
           vertical-align:top;
           display:inline;}

img.link {vertical-align: middle;
          width:32px;
          height:32px;}

/* Main article titles, site title */
h1 {text-align: center;
    font-style: italic;
    font-family: serif;
    /* Change the following to a lighter color for better readability */
    color: #313131;
   }
/* h2 – second-level headings (sub-titles, section titles, etc.) */
h2 {text-align:center;
    color: #7352E6;
    font-family:monospace;}
/* h3 – third-level headings (sub-sections, subsections, etc.) */
h3 {text-align:center;
    color: #aca4f6;
    font-family:monospace;}

strong {font-weight: 700;
        color: #C5D5D5;}

em, cite {color:#aca4f6;
          font-family:serif;}

em.hrec {font-weight: 700;
         font-style: italic;
         color: lime;}

em.rec {font-weight: 700;
        color: greenyellow;
        font-style: normal;}

em.srec {font-weight: 500;
         color: yellow;
         font-style: normal;}

em.nrec {font-weight: 400;
         color: orange;
         font-style: normal;}

em.avoid {font-weight: 700;
          color: red;
          font-style: italic;}

q, blockquote {font-style: italic;
               color: #C5D5D5;
               font-family:serif;}

#articlelist {margin-top: 20px;
              font-family: serif;
              text-align: center;
              padding: 5px;}

table {border: 0;}

td {border: #f8f8f8 solid 1px;
    padding-left: 2px;
    padding-right: 2px;
    padding-top: 0;
    padding-bottom: 0;
    background-color: #313131;
    color: #f8f8f8;}

.toprow {background-color: #7352E6;
         color: #f8f8f8;}

.good {background-color:greenyellow;
       color:black;}

.neutral {background-color:gold;
          color:black;}

.bad {background-color:crimson;}

#+end_src
